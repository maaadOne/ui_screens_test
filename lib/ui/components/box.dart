import 'package:flutter/material.dart';

class HBox extends SizedBox {
  const HBox(double height) : super(height: height, width: 0);
}

class WBox extends SizedBox {
  const WBox(double width) : super(height: 0, width: width);
}
